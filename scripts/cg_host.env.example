# Copy to scripts/cg_host.env (untracked) and fill in the aarch64 host address.
# scripts/cg.sh and sync_to_cg.sh source it when CG_HOST is not already set.
# With tailscale up, the MagicDNS name of the node works as well as its 100.x IP.
CG_HOST=
